# GoldMind — شرح كامل للمشروع

ده شرح تفصيلي لكل حاجة اتعملت في مشروع توقع سعر الذهب، وليه اتعملت بالشكل ده،
عشان يبقى عندك مرجع تقدر ترجعله في أي وقت.

---

## آخر تحديثات (مراجعة شاملة قبل الرفع النهائي)

### مراجعة خامسة: السعر ميتغيرش أكتر من مرة في اليوم

سبب حقيقي: مصدر السعر المحلي (elomla.com/GoldRate24) بيرجّع آخر نقطة
في سلسلة **يومية**، وسعر الذهب بالتجزئة في مصر بيتحدد رسميًا مرة أو
مرتين في اليوم من السوق المحلي - مش تيكر مستمر زي الفوركس/العقود
العالمية. يعني حتى مع بنية "live" شغالة صح 100%، لو المصدر نفسه لسه
مايتحدثش، أي refresh هيرجع نفس الرقم لحد ما المصدر يحدّث فعليًا.

**الحل المُنفّذ**: `_get_live_snapshot_inner()` في `predictor.py` بقى:
1. يقارن السعر المحلي اللحظي بإغلاق أمس.
2. لو **نفس القيمة** (يعني المصدر المحلي لسه ما حدّثش النهاردة)، بيحسب
   **تقدير** للسعر الحالي بناءً على حركة الذهب العالمي وسعر الدولار
   اللحظيين (اللي فعلًا بيتحركوا باستمرار خلال اليوم) - بافتراض إن
   هامش الربح المحلي (premium) ثابت تقريبًا على مدى ساعات قليلة.
   الـ status في الحالة دي بيبقى `estimated_live` (مُوسوم بوضوح في
   الواجهة "تقدير لحظي" - مش نفس شارة "مباشر" العادية) عشان مايتلخبطش
   مع رقم رسمي فعلي.
3. لو المصدر المحلي فعلًا حدّث (رقم مختلف عن أمس)، بيستخدم الرقم
   الحقيقي مباشرة من غير أي تقدير.

اتأكد بالاختبار الفعلي: 3 سيناريوهات (تقدير لما العالمي يتحرك، ثبات
لما العالمي يفضل زي ما هو، واستخدام الرقم الحقيقي لو المصدر المحلي
حدّث فعلًا) - الثلاثة اشتغلوا صح.



### مراجعة رابعة (بعد أول ديبلوي حي على Vercel)

1. **باج "نفس الرقم في كل الكروت" (6,349 متكرر)** - ده مش باج frontend
   فعليًا، كان نتيجة مباشرة للمعايرة (`blend_alpha`) اللي طلعت من
   التقييم: الأفق 1 يوم و7 أيام كانوا `alpha=0.00` (يعني "أحسن تخمين =
   السعر هيفضل زي ما هو")، وده خلّى توقع بكرة وكل أيام الأسبوع تساوي
   السعر الحالي بالظبط - رقم صحيح إحصائيًا بس بيبان زي باج للمستخدم.
   **الحل**: `MIN_BLEND_ALPHA = 0.15` في `predictor.py` - حد أدنى بيضمن
   إن الموديل دايمًا يساهم بجزء حقيقي من إشارته حتى في الآفاق القصيرة،
   فالتوقعات بقت تتغير يوم عن يوم (فروقات صغيرة، وده منطقي وصادق لسعر
   الذهب قصير المدى) بدل خط مسطّح. اتأكد بالاختبار الفعلي.
2. **نتيجة "اسأل عن أي تاريخ"**: بقت مقسّمة لأسطر واضحة بعناوين منفصلة
   (التاريخ / السعر الحالي للمقارنة / المدى المتوقع) بدل سطر واحد
   مدموج.
3. **ألوان الـ charts بقت TradingView-style فعلي** - مش لون واحد ثابت:
   كل خط بقى **مقسّم لأجزاء ملوّنة** (أخضر لما السعر/التوقع يطلع من
   النقطة اللي قبله، أحمر لما ينزل) باستخدام خاصية `segment` بتاعة
   Chart.js v4 - ده بيتطبق على كل الرسومات (توقع الأسبوع، توقع السنة،
   السعر الفعلي شهر/سنة). كمان ضفت badge ملوّن (+/-) جنب أرقام "بكرة"
   و"الأسبوع القادم" بيوضح الفرق عن السعر الحالي بالجنيه والنسبة.



### مراجعة ثالثة: refresh vs retrain + معايرة حقيقية للدقة

1. **"زرار الـ refresh يعيد تدريب الموديل"** - اتفحصت الفكرة دي وطلع إنها
   مش الحل الصح فعليًا (التفاصيل والأسباب في README تحت "ليه الـ refresh
   مش بيعيد تدريب الموديل"). البديل الصح (وهو أصلًا اللي شغال) اتفحص
   وأتأكد منه فعليًا: جرّبت سيناريوهين بسعر لحظي مختلف تمامًا، وأكدت إن
   السعر المعروض والتوقعات كلها بتتغير فعلًا مع كل قيمة جديدة، وإن الـ
   cache (20 ثانية) بيشتغل صح (مايكررش الطلب لو اتضغط أكتر من مرة بسرعة)
   وإن `force=1` (زرار الـ refresh) بيتخطى الـ cache ده في كل مرة.
2. **معايرة حقيقية لتحسين الدقة** - `train_model.py` بقى فيه خطوتين
   إضافيتين بعد التدريب:
   - **Shrinkage/blend نحو الـ baseline**: بما إن التقييم أثبت إن الموديل
     الخام بيخسر قدام "افرض إن السعر هيفضل زي ما هو" في آفاق 1-180 يوم،
     بقى فيه `blend_alpha(h)` محسوب فعليًا من نفس بيانات التقييم
     (held-out حقيقي) بيمزج توقع الموديل مع الـ baseline بالنسبة
     المثلى - والنتيجة: الموديل بعد المعايرة **بقى يتفوق على الـ
     baseline في كل الآفاق** (كان قبل كده بيخسر في 1-180 يوم).
   - **معايرة عرض نطاق عدم اليقين (`band_scale`)**: هنا حصلت مشكلة
     حقيقية لقيتها بالاختبار الفعلي - المقياس اللي طلع من موديلات
     التقييم الصغيرة (المؤقتة) لما اتطبق على الموديل الحقيقي المنشور،
     طلّع **أسعار سالبة** في آفاق طويلة (180+ يوم)! السبب: موديلات
     التقييم أصغر وبتدي نطاق أضيق، فمضاعفة كبيرة عليها من غير تحقق
     كارثة لو طُبّقت على موديل أعرض نطاق أصلًا. **الحل**: معايرة
     `band_scale` بقت بتتحسب من الموديل الحقيقي المنشور نفسه (مش
     موديل تجريبي)، + حماية قصوى (hard clamp) في `predictor.py` بتمنع
     ظهور أي سعر سالب أو نطاق عبثي **بغض النظر عن جودة المعايرة نفسها**.
     اتأكد بالاختبار إن كل الأسعار عبر كل الآفاق (1 لـ 400 يوم) منطقية
     دلوقتي.
   - **بصراحة تامة**: حتى بعد المعايرة، تغطية الـ uncertainty band في
     آفاق 90+ يوم لسه أقل من الـ 80% المستهدفة (بين 50-70% حسب الأفق).
     يعني النطاق المعروض للمستخدم في التوقعات الطويلة **متفائل شوية عن
     الواقع** - المخاطرة الحقيقية أكبر من الظاهر على الشاشة. ده قرار
     واعي (تفضيل نطاق معقول وآمن على نطاق واسع جدًا مبني على تخمين غير
     محقق) مش إهمال - وتفاصيله كاملة في `models/meta.json`.
3. **ملف `05_MACRO`** اتشال نهائيًا من المشروع - طلع إنه بيانات World
   Gold Council الشهرية لسعر الذهب بالجنيه المصري من 1978، لكنها:
   (أ) شهرية مش يومية، (ب) متكررة فعليًا مع feature موجود أصلًا
   (`Gold_World × USD_EGP`)، (ج) ملهاش فايدة حقيقية لأن الـ target نفسه
   (سعر عيار 21 المحلي) أصلًا مبيبدأش قبل 2013 - فمفيش داعي لبيانات
   تاريخية أقدم من كده.



### مراجعة شاملة تانية (بعد رفع المشروع على GitHub + Vercel فعليًا)

بعد ما المشروع اتربط بـ GitHub وعليه Vercel deployment، ظهرت مشاكل حقيقية
جديدة اتفحصت واتصلحت كل واحدة فيها بشكل مستقل (مش بس نقل كلام):

1. **السبب الحقيقي وراء اختفاء الـ CSS على Vercel** — Flask's
   `app.static_folder` **مش مدعوم لتقديم static files على Vercel**
   (مؤكد من [توثيق Vercel الرسمي لـ Flask](https://vercel.com/docs/frameworks/backend/flask),
   محدّث 2026-08-12: "Flask's app.static_folder should not be used for
   static files on Vercel. Use the public/** directory instead"). كل
   ملفات `static/css`, `static/js`, `static/vendor` اتنقلت لـ `public/`
   (اللي Vercel بيقدّمها مباشرة من الـ CDN بتاعه)، الروابط في
   `index.html` اتغيّرت لمسارات مباشرة (`/css/style.css` بدل
   `url_for('static', ...)`)، و`vercel.json` اتبسّط بالكامل للصيغة
   الحديثة (Zero-config Flask) بدل صيغة `builds`/`routes` القديمة.
2. **GPR كان بيفشل تحميله دايمًا** — الملف اللي بيتحمّل `.xls` (صيغة
   قديمة) مش `.xlsx`، ومحتاج مكتبة `xlrd` تحديدًا (`openpyxl` بيقرا
   `.xlsx` بس). اتأكد الخطأ ده فعليًا محليًا (`ImportError: Import xlrd
   failed...`)، واتصلح بإضافة `engine="xlrd"` + المكتبة في
   `requirements-dev.txt`.
3. **الـ workflow كان بيبلع فشل أي مصدر بيانات** — لو GoldRate24 أو
   Yahoo أو FRED فشلوا، الكود كان يكمل عادي ويعمل commit وكأن مفيش
   حاجة حصلت (GitHub بيقول ✅ Success حتى لو الموديل اتدرب على بيانات
   قديمة). **اتصلح**: أي مصدر أساسي فاشل دلوقتي بيوقف السكريبت
   (`SystemExit(1)`) قبل الـ build/train/commit. GPR وحده اتسيب
   "optional" (تحذير بس) لأنه أصلًا مش داخل في الـ features لحد دلوقتي.
4. **Git repo كان بيكبر بشكل غير ضروري** — `data/processed/merged_dataset.csv`
   (~19-20MB) كان بيتعمله commit كامل من الصفر كل يوم رغم إنه قابل
   لإعادة البناء 100% من `data/raw/`. اتضاف لـ `.gitignore` (الـ
   workflow لسه بيبنيه على الـ runner وقت التدريب، بس مش بيتعمله
   commit).
5. **GitHub Actions قديمة** — `actions/checkout` و`actions/setup-python`
   اترفعوا لـ v7 (أحدث إصدار حاليًا، Node 24).
6. **"Staircase" في منحنى توقعات السنة** — الموديل كان مدرّب على 23
   horizon بس (متباعدين)، لكن `/api/year-forecast` بيسأل عن نقطة كل 7
   أيام. النتيجة: 37 من 52 نقطة كانت متطابقة تمامًا (خط مسطّح بدل منحنى).
   **اتصلح** بإعادة التدريب على 59 horizon يغطوا بالظبط كل نقطة بيسألها
   التطبيق - النتيجة بقت نقطة واحدة بس متطابقة.
7. **تقييم حقيقي للموديل (مش بس رقم واحد)** — `train_model.py` كان
   بيقيس بس دقة يوم واحد قدام، من غير أي مقارنة بـ baseline. اتضاف
   walk-forward evaluation حقيقي عبر 6 آفاق (1، 7، 30، 90، 180، 365
   يوم) بمقارنة صريحة مع baseline بسيط ("السعر هيفضل زي ما هو")، وحساب
   MAE / RMSE / MAPE / Directional Accuracy / Quantile Coverage. النتيجة
   محفوظة في `models/meta.json["evaluation"]` - **وهي نتيجة مهمة لازم
   تتقرا بصراحة**: الموديل حاليًا **مش بيتفوق على الـ baseline** في آفاق
   1-180 يوم (تحسّن سالب)، وبس في أفق 365 يوم بيتفوق فعلاً (+36.4%). ده
   مش عيب برمجي - ده انعكاس لصعوبة حقيقية معروفة في التنبؤ المالي
   قصير/متوسط المدى (سعر الذهب قريب من "random walk" في المدى القصير).
   كمان الـ uncertainty band (10%-90%) بتاعت الموديل ضعيفة في الآفاق
   الطويلة (تغطية 15% بس في أفق 365 يوم بدل الـ 80% المفروضة) - يعني
   المدى المعروض للمستخدم متفائل زيادة عن اللازم في التوقعات البعيدة.
8. حاجات تانية أصغر: فصل `requirements.txt` (إنتاج) عن
   `requirements-dev.txt` (تطوير/pipeline) عشان حجم الـ deployment على
   Vercel، تصحيح line endings متضاربة، تصحيح label في الواجهة لما السعر
   يبقى `partial_live`، تسريع جلب الأسعار اللحظية بالتوازي بدل التتابع.



في مراجعة شاملة لكل السطور قبل النسخة دي، اتلاقى وتصلح 3 حاجات مهمة:

1. **باگ حرج في مسار البيانات** — `update_gold_project.py` كان بيحفظ
   الداتا الجديدة في فولدر اسمه `Gold_ML_Project_Data/`، لكن
   `build_dataset.py` بيقرا من `data/raw/`. المعنى العملي: الـ GitHub
   Action اليومي كان هيحمّل بيانات جديدة كل يوم في مكان الموديل مش
   بيشوفه، فالموديل كان هيفضل "متجمد" على نفس البيانات القديمة للأبد
   حتى بعد سنين من التشغيل. **اتصلح** بخلي المسار يتحسب نسبيًا لمكان
   الملف نفسه، بحيث يطابق بالظبط المكان اللي `build_dataset.py` بيدور
   فيه.
2. **باگ تواريخ "بكرة"** — كانت بتتحسب من آخر تاريخ في بيانات التدريب
   (`meta.json`) مش من تاريخ النهاردة الحقيقي، فلو الموديل معملوش
   retrain لبضعة أيام، "بكرة" كانت ممكن تظهر بتاريخ *قبل* النهاردة
   فعليًا. **اتصلح** في `model/predictor.py`: تاريخ "النهاردة" المستخدم
   لحساب كل التواريخ المعروضة بقى دايمًا `date.today()` الحقيقي، بغض
   النظر عن حداثة بيانات الـ features نفسها.
3. **الاعتماد على CDN خارجي للـ charts** — Chart.js كان بيتجاب من رابط
   خارجي (cdnjs.cloudflare.com)، ولو الشبكة عند حد فيها أي مانع (إضافة
   متصفح، فايروول) الـ charts كانت بتفضل فاضية. **اتصلح** بتضمين
   المكتبة جوه المشروع نفسه في `static/vendor/chart.umd.min.js`.

الموديل اتعاد تدريبه بالكامل من الصفر بعد التصليحات دي (نفس البيانات،
كود مُتحقق منه بالكامل)، واتعمله full end-to-end test في بيئة Python
نضيفة (virtualenv من الصفر) بمحاكاة قريبة جدًا لبيئة الإنتاج، وكل
الـ endpoints ورجعت نتايج صحيحة من غير أي error.

### تغيير الاسم لـ GoldMind + اللوجو + مؤشر مخاطر مُشتق

بعد طلب تغيير اسم المشروع وسؤال عن مؤشرات المخاطر السياسية/الاقتصادية:

- **الاسم اتغيّر لـ GoldMind** في كل مكان (الفولدر نفسه، الصفحة، الـ
  README، كل التوثيق) - اتأكد بالـ `grep` إن مفيش أي أثر للاسم القديم
  فضل في أي ملف.
- **لوجو SVG متجه** جوه الصفحة مباشرة (مش صورة خارجية): دائرة بتدرج
  ذهبي فيها أعمدة صاعدة (تمثيل لرسم بياني السعر) متوصلة بنقط شبكة
  عصبية (تمثيل لـ "الذكاء" في الاسم) - يشتغل بنفس الجودة في أي حجم.
- **مؤشر مخاطر مُشتق شغال فعليًا الآن** (`market_stress_proxy` +
  `gold_safehaven_signal` في `feature_utils.py`): بما إني معنديش وصول
  لمؤشرات GPR/EPU الحقيقية (محتاجة إنترنت مش متاح في بيئتي)، بنيت
  بديل عملي من بيانات موجودة فعلًا - تقلب VIX+البترول+الدولار مجمّع
  (مؤشرات الأحداث السياسية بتظهر كتقلب مفاجئ في السوقين دول)، وفرق أداء
  الدهب عن الأسهم (إشارة "الهروب للأمان" الكلاسيكية وقت الأزمات). الاتنين
  اتضافوا فعليًا للتدريب (198 عمود بدل 196) واتأكد إنهم بيشتغلوا في
  التوقع اللحظي بنفس الصيغة (single source of truth). كود تحميل GPR/EPU
  الحقيقي فضل موجود وجاهز فى `update_gold_project.py` لحد ما يتوفر
  إنترنت فعلي.

### جولة audit تانية (بعد طلب فحص شامل إضافي)

عملت جولة فحص تانية أعمق، فكّيت الـ zip المُسلَّم في مكان جديد تمامًا
(مش هيستخدم أي حالة قديمة في الذاكرة) وشغّلت عليه اختبارات ضغط (stress
tests) تحاكي سيناريوهات فشل حقيقية، مش بس "المسار السعيد". لقيت نقطة
ضعف واحدة إضافية واتصلحت:

4. **مفيش حماية من عطل غير متوقع في مصادر البيانات اللحظية** — لو حصل
   أي خطأ مش متوقع خالص (مش مجرد "فشل الاتصال" العادي اللي كان متغطي)
   جوه منطق جلب الأسعار اللحظية، كان ممكن يوقّع الـ endpoint كله برسالة
   خطأ تقنية (500 مع traceback). **اتصلح** بإضافة طبقة حماية شاملة في
   `get_live_snapshot()` بترجع للـ snapshot الآمن المخزّن تلقائيًا لو
   حصل أي حاجة غير متوقعة، بالإضافة لـ error handler عام في `app.py`
   بيتأكد إن أي عطل في أي endpoint بيرجع JSON نضيف (`{"error": "..."}`)
   بدل صفحة HTML فيها تفاصيل داخلية.

**اختبارات اتعملت فعليًا في الجولة دي:**
- إعادة تدريب كامل من الصفر في virtualenv جديد + نفس دقة الموديل
- محاكاة الحالات الأربع: نجاح كامل / نجاح جزئي / فشل شبكة عادي / عطل
  غير متوقع تمامًا - الأربعة رجعوا نتيجة صحيحة من غير كسر الموقع
- محاكاة ملف موديل تالف/مفقود - رجع JSON نضيف بدل الكراش
- محاكاة حزمة النشر بتاعة Vercel (تطبيق `.vercelignore` فعليًا) للتأكد
  إن كل ملف محتاجه الموقع وقت التشغيل موجود، ومفيش ملفات زيادة اتنسيت

### جولة audit تالتة (زرار Refresh + دعم لغتين + فحص شامل إجباري)

بعد طلب إضافة زرار تحديث يدوي، وترجمة الموقع للإنجليزية، وفحص شامل تاني
من أول المشروع لآخره، اتعمل الآتي:

**الميزات الجديدة:**
- **زرار Refresh** في الهيدر (أيقونة دائرية بتلف وقت التحميل) - بيعمل
  force-refresh حقيقي: بيتجاوز الـ 20-ثانية cache بتاعة البيانات اللحظية
  ويجيب أرقام جديدة فعليًا من live_price.py/live_quotes.py، مش بس يعيد
  تحميل نفس البيانات المخزّنة. مربوط بكل الـ 4 endpoints (`summary`,
  `year-forecast`, `history` x2) عن طريق `?force=1` query param.
- **دعم لغتين (عربي/إنجليزي)** - زرار تبديل بجانب زرار الوضع الليلي.
  التبديل بيغيّر: كل النصوص، اتجاه الصفحة (RTL↔LTR تلقائي، مفيش أي CSS
  مكسور لأن التصميم كله flexbox+gap من غير أي خاصية اتجاه صريحة)، تنسيق
  الأرقام (فواصل عربي/إنجليزي)، ورمز العملة (ج.م ↔ EGP). الاختيار
  بيتحفظ في المتصفح (`localStorage`) ولما ترجع تفتح الموقع بيفتح بنفس
  اللغة اللي اخترتها.

**نقطة ضعف إضافية اتلاقت واتصلحت أثناء اختبار زرار الـ Refresh:**

5. **نقطة "فعلية" مضلّلة في شارت التاريخ** - أثناء اختبار الحل بتاع
   مشكلة الـ charts المتأخرة (القسم اللي فات)، لقيت إن الكود كان بيضيف
   نقطة "النهاردة" في الشارت حتى في حالة `cached` الكاملة (يعني لما
   السعر اللحظي نفسه فشل جلبه) - وده كان بيكرر آخر سعر قديم معروف
   ويحطله تاريخ النهاردة، فيوهم اليوزر إن السعر معروف فعليًا وإنه ثابت،
   والحقيقة إننا مش عارفين سعر النهاردة خالص. **اتصلح** بخلي إضافة نقطة
   "النهاردة" تحصل بس لو الحالة `live` أو `partial_live` (يعني فيه سعر
   لحظي حقيقي اتجاب فعلًا)، مش في حالة `cached`.

**اختبارات الجولة دي:**
- تدريب كامل من الصفر بعد إضافة الميزتين - نفس الدقة
- محاكاة زرار الـ refresh end-to-end (`?force=1` على كل الـ endpoints)
- محاكاة النقطة المضلّلة قبل وبعد التصليح - اتأكد إنها مش بتظهر في
  حالة `cached` وبتظهر صح في حالة `live`/`partial_live`
- فحص قاموس الترجمة بالكامل: 33 مفتاح في العربي و33 في الإنجليزي
  (مطابقة تمامًا، مفيش نقص)، وكل الـ `data-i18n` في الـ HTML ليها
  مقابل في القاموس (اتأكد بمقارنة آلية، مش بالعين)
  - فحص شامل لأي نص عربي متسرب في JS أو HTML من غير تغطية بنظام
  الترجمة - مفيش أي نص متسرب

---


## 1. الفكرة العامة

المطلوب كان: موديل ML يتعلم من بيانات تاريخية ويتوقع سعر الذهب عيار 21 في
مصر — بكرة، الأسبوع الجاي، وأي تاريخ لحد سنة قدام — وده كله ورا web app
عصري بواجهة dark/light، متزامن مع السعر الحالي، وجاهز للرفع على GitHub
ثم Vercel.

المشروع اتقسم لـ 3 طبقات منفصلة عن بعض بشكل واضح:

```
data_pipeline/  →  model/  →  app.py (Flask) + templates/static (UI)
   (تجهيز الداتا)   (تدريب/توقع)      (يعرض النتيجة للمستخدم)
```

الفصل ده مهم عشان كل طبقة تتعدل لوحدها من غير ما تكسر التانية — لو حبيت
تضيف مصدر بيانات جديد مثلاً، بتلمس `data_pipeline/` بس.

---

## 2. `data_pipeline/` — تجهيز الداتا

### `update_gold_project.py` (سكريبتك الأصلي + إضافات)
ده نفس السكريبت اللي بعتهولي، وضفتله قسم جديد (`06_EVENTS`) بيحمّل:

- **EPU (Economic Policy Uncertainty)** من FRED — بالظبط بنفس الطريقة اللي
  كنت بتحمّل بيها `DGS10` (عن طريق `fredgraph.csv`)، بس بسلاسل تانية:
  - `USEPUINDXD` (يومي، أمريكا)
  - `GEPUCURRENT` (شهري، عالمي)
- **GPR (Geopolitical Risk Index)** من موقع الباحثين اللي ابتكروه (Caldara
  & Iacoviello - اقتصاديين في الـ Federal Reserve) — ده مؤشر أكاديمي معروف
  بيحوّل الأحداث السياسية (حروب، توترات، تهديدات) لرقم واحد قابل للتعلم،
  بدل ما نخترع "تقييم" يدوي لكل حدث.

المصدرين دول هما إجابة سؤالك الأصلي عن "الأحداث السياسية/الاقتصادية" —
مفيش داعي تعمل feature يدوي لكل حدث، الرقمين دول بيلخصوا الموضوع كله.

**ملحوظة:** السكريبت ده محتاج إنترنت مباشر وقت التشغيل (Yahoo, FRED,
matteoiacoviello.com, elomla.com). في بيئة التطوير اللي بنيت فيها المشروع
مفيش وصول لمواقع دي، فاختبرت السكريبت بـ syntax check بس مش تشغيل فعلي
للتحميل. هيشتغل عادي لما تشغّله من جهازك أو من GitHub Actions.

### `build_dataset.py` (جديد بالكامل)
ده اللي بياخد كل الملفات الخام (من `data/raw/`) ويحوّلهم لجدول واحد
جاهز للتدريب. أهم قرارات اتخدت فيه:

- **الـ calendar**: بنياخد تواريخ TARGET (سعر الذهب المصري) كأساس لأنه
  الوحيد اللي عنده بيانات كل يوم في الأسبوع (حتى الجمعة والسبت). باقي
  المصادر (Yahoo/FRED) عندها بيانات أيام التداول بس، فبنعمل لها
  `forward-fill` — يعني آخر سعر معروف بيفضل "ساري" لحد ما يظهر سعر جديد.
  دي الطريقة القياسية لما تدمج بيانات retail مستمرة مع بيانات سوق ليها
  إجازات.

- **`local_premium_ratio`**: عمود جديد بيحسب الفرق بين سعر الذهب المصري
  الفعلي والقيمة "المفترضة" (سعر الذهب العالمي × سعر الدولار). ده feature
  مفيد لأنه بيمسك أي "علاوة محلية" (ندرة، طلب، ضرائب) مش موجودة في
  الأسعار العالمية نفسها.

- **الـ features لكل عمود سعري**: لكل مصدر (Gold_World, USD_EGP, DXY,
  WTI, إلخ) بنحسب: log price، log return يومي، lag returns (1، 2، 3، 5،
  7، 14، 21، 30، 60، 90 يوم)، ومتوسط/انحراف معياري متحرك (rolling) على
  نوافذ 7/14/30/60 يوم. المجموع طلع 196 عمود من 9 مصادر خام بس.

- **حالة خاصة**: سعر البترول (WTI) نزل *سالب* فعليًا في أبريل 2020 (حدث
  تاريخي مشهور)، وده كان بيكسر حساب الـ log. اتعالجت بـ clip بسيط.

**النتيجة**: `data/processed/merged_dataset.csv` — 4,909 يوم (من
2013-03-27 لحد 2026-09-03) × 196 عمود.

---

## 3. `model/` — التدريب والتوقع

### الفكرة الأساسية: "Horizon كـ Feature"
بدل ما نعمل موديل منفصل لكل مدة توقع (موديل لبكرة، موديل تاني للأسبوع،
تالت للسنة...) وده صعب الصيانة وغير عملي لمدى لحد سنة، عملنا **موديل
واحد** بياخد "بعد كام يوم عايز تتوقع" (`horizon`) كـ input عادي زي أي
feature تاني. مثال:

```
مدخلات الموديل = [كل الـ lag/rolling features بتاعت النهاردة] + [horizon=7]
مخرجات الموديل = log-return متوقع بعد 7 أيام
السعر المتوقع = السعر الحالي × exp(log-return المتوقع)
```

كده لما حد يسأل عن أي تاريخ، بنحسب `horizon = عدد الأيام بينه وبين النهاردة`
ونمرره للموديل، وخلاص. اتدرّب على 59 horizon (كل يوم من 1 لـ 7، وبعدين كل
7 أيام لحد 365) بدل 23 نقطة متباعدة زي الأول - عشان أي نقطة يسألها
التطبيق فعليًا (مش بس نقاط "ممثّلة") يبقى الموديل شافها وقت التدريب، وده
اللي بيمنع ظهور "درجات سلّم" (قيم متطابقة) في منحنى توقعات السنة.

### ليه HistGradientBoostingRegressor مش LSTM أو LightGBM؟
- **مش LSTM/Deep Learning**: البيانات دي (جدولية، مدفوعة بمؤشرات اقتصادية)
  بتتوقع أحسن بموديلات الـ gradient boosting في الغالبية العظمى من
  الحالات العملية (مثبت في مسابقات زي M5 forecasting)، وأخف وأسرع بكتير.
- **مش LightGBM/XGBoost**: دول محتاجين مكتبات native منفصلة ممكن تعمل
  مشاكل توافق وقت الـ deployment على Vercel serverless. استخدمنا
  `HistGradientBoostingRegressor` من scikit-learn نفسها — نفس الفكرة
  والأداء، بس من غير أي تعقيد إضافي وقت النشر.

### الـ Uncertainty (مدى الثقة)
بندرّب **3 نسخ** من نفس الموديل بـ quantile loss مختلف (10%، 50%، 90%).
كده بدل ما نقول "السعر بكرة هيبقى 6350 جنيه" (رقم كاذب الدقة)، بنقول
"الأرجح 6350، والمدى الواقعي بين 6280 و 6420". ده مهم جدًا للتوقعات
البعيدة (سنة قدام) اللي فيها عدم يقين طبيعي كبير.

### `predictor.py`
الملف اللي بيحمّل الموديلات المدرّبة (مرة واحدة بس لما السيرفر يشتغل، مش
في كل request) ويعمل التحويل من log-return لسعر فعلي. فيه 4 functions
رئيسية: توقع لمدة معينة، توقع لتاريخ معين، توقع الأسبوع الجاي كامل،
وتوقع منحنى السنة الجاية.

### `live_price.py`
بينادي نفس API بتاعك (elomla.com) عشان يجيب السعر الحالي وقت الطلب. لو
فشل الاتصال لأي سبب (الموقع واقع، مشكلة شبكة)، بيرجع للسعر المخزّن آخر
مرة اتدرب فيها الموديل بدل ما يكسر الصفحة كلها.

### `train_model.py`
بيبني جدول التدريب (بيوسّع كل يوم لعدة صفوف حسب الـ horizons)، يدرّب
الـ 3 موديلات، ويعمل **تقييم walk-forward حقيقي عبر 6 آفاق مختلفة** (1،
7، 30، 90، 180، 365 يوم) بمقارنة صريحة مع baseline بسيط، ويحفظ كل حاجة
في `models/` (النتايج الكاملة في `models/meta.json["evaluation"]`).

**النتيجة الفعلية اللي طلعت من التدريب:** 273,699 عينة تدريب. أهم حاجة
تعرفها: الموديل **بيتفوق بوضوح على baseline بسيط ("السعر هيفضل زي ما
هو") بس في أفق سنة كاملة (365 يوم، تحسّن +36.4%)** - في آفاق أقصر
(1-180 يوم) التحسّن سالب حاليًا، يعني الـ baseline البسيط بيغلط أقل من
الموديل. ده مش عيب برمجي، ده انعكاس صادق لصعوبة معروفة في التنبؤ
المالي قصير/متوسط المدى. راجع قسم "آخر تحديثات" فوق للتفاصيل الكاملة.

---

## 4. `app.py` — الـ Backend (Flask)

الـ API بسيط ومباشر، 5 endpoints:

| Endpoint | الوظيفة |
|---|---|
| `GET /` | يعرض الصفحة الرئيسية |
| `GET /api/summary` | السعر الحالي + توقع بكرة + توقع الأسبوع كامل |
| `GET /api/year-forecast` | منحنى توقعات السنة الجاية (53 نقطة أسبوعية) |
| `GET /api/history?range=1y\|1m` | الأسعار الفعلية (مش متوقعة) للسنة أو الشهر الفايت |
| `GET /api/predict?date=YYYY-MM-DD` | توقع لتاريخ مخصص، مع validation (يرفض تواريخ ماضية أو أبعد من ~400 يوم) |

كل endpoint بيستخدم السعر الحالي (live أو fallback) كنقطة انطلاق (anchor)
لحساب التوقعات، مش السعر المخزّن وقت التدريب بس — كده التوقع دايمًا
"واقعي" بالنسبة للحظة اللي المستخدم بيشوف فيها الصفحة.

---

## 5. الـ Frontend (`templates/` + `public/`)

- **التصميم**: مستوحى من الصورة اللي بعتهالي — dark navy أساسي، مع لمسة
  ذهبية (gold accent) بدل التركواز اللي في الصورة الأصلية، عشان يناسب
  موضوع المشروع. فيه وضع فاتح (light) كامل كمان.
- **الخطوط**: Newsreader (سيريف أنيق) للعناوين، JetBrains Mono (مونوسبيس)
  لكل الأرقام — ده بيخلي الأرقام تتصف صح وتدي إحساس "شاشة تداول احترافية".
- **الـ Cards العلوية**: السعر الحالي، توقع بكرة (مع مدى الثقة)، توقع
  الأسبوع الجاي (مع مدى الثقة) — بالظبط زي ما طلبت.
- **الـ Charts** (بمكتبة Chart.js): توقعات الأسبوع الجاي، السنة الفايتة
  الفعلية، الشهر الفايت الفعلي، توقعات السنة الجاية (مع band لوني بيمثل
  مدى عدم اليقين).
- **قسم التاريخ المخصص**: input بتاريخ + زرار، بينادي `/api/predict`
  ويعرض النتيجة.
- **زرار dark/light**: بيحفظ اختيارك في `localStorage` عشان يفضل زي ما
  سبته لما ترجع تفتح الموقع.

---

## 6. الرفع والتشغيل التلقائي

- **`vercel.json`**: Zero-config Flask - Vercel بيكتشف `app.py` تلقائي
  من غير أي `builds`/`routes` (الصيغة القديمة كانت هي سبب اختفاء الـ
  CSS - شوف قسم "آخر تحديثات" فوق). ملفات `public/` بتتقدّم مباشرة من
  الـ CDN بتاع Vercel، مش عن طريق Flask.
- **`.vercelignore`**: بيستبعد الملفات التقيلة اللي الموقع مش محتاجها
  وقت التشغيل (زي `merged_dataset.csv` اللي 19MB، والداتا الخام، وسكريبت
  التدريب) عشان الـ deployment يفضل خفيف وسريع.
- **`.github/workflows/daily_update.yml`**: GitHub Action بيشتغل يوميًا
  (3:30 صباحًا UTC) أو يدويًا من تبويب Actions، بيعمل: تحميل بيانات جديدة
  → إعادة بناء الـ dataset → إعادة تدريب الموديل → commit + push. أي
  push جديد بيخلي Vercel يعمل redeploy لوحده تلقائيًا — من غير أي
  database منفصل أو تدخل يدوي منك.

---

## 7. التحديث اللحظي للـ Features (تحسين إضافي بعد ملاحظتك)

بعد ما لاحظت إن باقي المؤشرات (مش السعر بس) يفضل يبقى live، ضفت طبقة
جديدة كاملة بتحل الموضوع ده:

### `feature_utils.py` (ملف مشترك جديد في جذر المشروع)
كل معادلات الـ log/lag/rolling features بقت في مكان واحد بس، وبيستوردها
كل من `data_pipeline/build_dataset.py` (وقت التدريب) و`model/predictor.py`
(وقت الطلب اللحظي). ده مهم عشان لو غيرت معادلة يوم من الأيام، التدريب
والتوقع اللحظي هيفضلوا متطابقين تلقائيًا من غير ما تنسى تعدّل في مكانين.

### `model/live_quotes.py` (ملف جديد)
بيجيب سعر لحظي لكل مؤشر (Gold_World, USD_EGP, DXY, WTI, Silver, SP500,
VIX) من نفس مصدر Yahoo Finance اللي بيستخدمه سكريبتك الأصلي. كل مؤشر
بيتجاب لوحده، فلو واحد فشل (المصدر واقع مؤقتًا) الباقي بيشتغل عادي.

### `data/processed/recent_market_window.csv` (ملف جديد، خفيف ~30KB)
بدل ما نحتاج نرفع الـ 19MB بتوع كل التاريخ عشان نعيد حساب الـ features
اللحظية، بنحتفظ بآخر 200 يوم بس من الأسعار الخام (كافيين لحساب أطول
rolling window عندنا وهو 60 يوم + أطول lag وهو 90 يوم بأمان).

### `predictor.get_live_snapshot()` (المنطق الأساسي الجديد)
كل ما حد يفتح الصفحة أو يطلب توقع، الدالة دي بتعمل الآتي بالترتيب:
1. تجيب سعر الذهب اللحظي (زي الأول).
2. تجيب أسعار باقي المؤشرات اللحظية.
3. تاخد آخر 200 يوم من `recent_market_window.csv`، تضيفلهم صف "النهاردة"
   بالقيم اللحظية (وأي مؤشر فشل جلبه بياخد آخر قيمة معروفة بدل ما يبوّظ
   الطلب كله).
4. تعيد حساب كل الـ log/lag/rolling features بس لصف النهاردة، باستخدام
   `feature_utils.py` - نفس المعادلات بالظبط المستخدمة وقت التدريب.
5. ترجع "حالة" وضحة: `live` (كل حاجة اتجابت لحظيًا)، `partial_live`
   (بعض المؤشرات لحظي وبعضها احتياطي)، أو `cached` (فشل جلب سعر الذهب
   نفسه، فرجعنا بالكامل للـ snapshot المجمّد القديم زي الأول).

الواجهة (`main.js`) بتعرض الحالة دي في الـ pill اللي فوق ("مباشر" /
"مباشر (جزئي)" / "آخر سعر مسجل") عشان تكون شفاف مع اليوزر عن مصدر
البيانات اللحظة دي بالظبط.

**اختبار مهم عملته:** بما إن بيئة التطوير بتاعتي مقفولة على الإنترنت
الخارجي (معنديش وصول لـ Yahoo/elomla هنا)، مقدرتش أختبر الـ fetch
الحقيقي، فعملت اختبار بديل: "زرعت" (mocked) قيم لحظية وهمية مكان الـ
API calls واتأكدت إن الكود بيحسب الـ features صح ويرجع النتيجة الصح
في 3 سيناريوهات: نجاح كامل، نجاح جزئي (مؤشر واحد فشل)، وفشل كامل
(fallback للـ snapshot القديم). الثلاثة اشتغلوا صح. أول ما ترفعه على
Vercel (فيه إنترنت فعلي)، الجزء الحقيقي بتاع الـ API calls هيشتغل
تلقائي من غير أي تعديل.

## 8. حاجات لازم تاخدها في اعتبارك (محدوديات حقيقية، مش كسل في الشغل)

1. **توقعات المدى البعيد أقل دقة بطبيعتها** — أي موديل هيتوقع سنة قدام
   هيكون عنده عدم يقين كبير، وده سبب وجود الـ low/high band بدل رقم واحد.
2. **~~الـ features غير السعرية بتتجمد عند آخر قيمة معروفة~~ (اتحلت)** —
   دلوقتي في `model/live_quotes.py` + `model/feature_utils.py` بيجيبوا
   أسعار لحظية فعلية (Gold_World, USD_EGP, DXY, WTI, Silver, SP500, VIX)
   وقت كل request ويعيدوا حساب الـ lag/rolling features بتاعت "النهاردة"
   على أساسها، بدل الاعتماد على آخر batch يومي بس. شوف قسم "التحديث
   اللحظي للـ Features" تحت لتفاصيل أكتر. الاستثناء الوحيد المتبقي هو
   `US10Y` (عائد سندات أمريكا 10 سنين) لأنه معندوش مصدر مجاني لحظي بسيط،
   وأصلاً مش بيتحرك بشكل مؤثر خلال اليوم الواحد.
3. **GPR/EPU لسه مش داخلين في التدريب الحالي** — الكود جاهز في
   `update_gold_project.py`، بس محتاج تشغيله بإنترنت فعلي (من جهازك أو
   GitHub Actions) عشان الملفات تتحمل، وبعدين تعيد `build_dataset.py` +
   `train_model.py` عشان الموديل يستفيد منهم فعليًا.
4. **السعر اللحظي بيعتمد على إن elomla.com يفضل شغال** — لو وقف، الموقع
   مش هيقع، هيرجع تلقائي لآخر سعر معروف (احتياط موجود في `live_price.py`).
